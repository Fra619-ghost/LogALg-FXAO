#include <stdio.h>
int main(int argc, char const *argv[])
{
    char name[20] = {"Francosi"};
    char letter = {'F'};
    printf("Su nombre es:%s",name);
    printf("Su letra inicial es:%c", letter);
    return 0;
}
